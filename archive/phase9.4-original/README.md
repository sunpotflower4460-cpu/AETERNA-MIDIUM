# Phase 9.4 Original

This directory preserves the pre-reorg single-file prototype as reference-only code.
The archived source is kept as `index.html.txt` so it stays readable without being treated as active runtime HTML.
