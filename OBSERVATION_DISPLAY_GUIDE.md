# 日本語観測表示機能（Japanese Observation Display）

## 概要

このプルリクエストでは、スマホで見た時に「何が起きているか分からない」という問題を解決するため、外部観測装置としての日本語説明表示機能を追加しました。

## 🎯 目的

- **外部観測装置として動作**: システムの内部状態を観測し、分かりやすい日本語で説明
- **インタラクションのフィードバック**: タッチ操作時に何が起きているかを即座に表示
- **システム状態の可視化**: QUIET、ACTIVE、CRITICALなどの状態変化を説明
- **数値は一切変更しない**: UI/UXのみの変更で、計算ロジックには影響なし

## ✨ 新機能

### 1. 観測表示パネル (`#observation-display`)

画面中央（モバイル）または下部（デスクトップ）に表示される半透明のパネル：

- **タッチ時の説明**:
  - 「刺激が伝わっています...」
  - 「ニューロンが活性化しました」
  - 「ネットワークに波が広がっています」

- **システム状態変化**:
  - 「システムが活動状態に入りました」
  - 「臨界状態に到達！バランスの取れた活動が生まれています」
  - 「過興奮状態：活動が活発すぎます」

- **プリセット適用時**:
  - 「🌍 シューマン共鳴モード：地球の周波数で安定した活動が始まります」
  - 「φ 黄金比モード：調和的で美しいパターンが生成されます」
  - 「⚡ ガンマ波モード：高周波で活発な神経活動が始まります」

- **重要なネットワークイベント**:
  - 「統合傾向が上昇：全体の秩序が高まっています」
  - 「同期パルス発生：ネットワーク全体が同期しています」
  - 「自己観測が活性化：システムが自己を認識しています」

### 2. 視覚的なフィードバック

- **色分けによる状態表示**:
  - 青系 (`touch-feedback`): タッチインタラクション
  - 黄色系 (`state-change`): 状態変化
  - 赤系 (`critical`): 臨界イベント

- **アニメーション**:
  - スムーズなフェードイン/アウト (0.3秒)
  - 各メッセージは2〜4秒間表示

### 3. レスポンシブデザイン

- **モバイル (≤768px)**:
  - 画面中央に配置（コントロールパネルと重ならない）
  - フォントサイズ: 12px
  - 幅: 85vw (最大350px)

- **デスクトップ (≥769px)**:
  - 画面下部中央に配置
  - フォントサイズ: 13px
  - 幅: 90vw (最大400px)

## 🔧 実装の詳細

### 新規ファイル

#### `src/ui/ObservationDisplay.js`
外部観測装置としての日本語説明表示を管理するクラス：

**主要メソッド**:
- `showTouchMessage(x, y)`: タッチ時の説明
- `showStateChange(newState, sigma)`: システム状態変化の説明
- `showPresetMessage(presetName)`: プリセット適用時の説明
- `showNetworkEvent(eventType, data)`: ネットワークイベントの説明
- `showModeChange(mode)`: モード変化の説明
- `showActionState(actionState)`: アクション状態の説明
- `showIntegrationChange(phi, threshold)`: 統合指標(Φ)の変化を説明

### 変更されたファイル

#### `index.html`
- 観測表示パネルのHTML要素を追加
- CSSスタイルを追加（レスポンシブ対応）

#### `src/main.ts`
- `ObservationDisplay`をインポート
- `state.observationDisplay`を初期化

#### `src/perception/pointerHandlers.js`
- タッチイベント時に`showTouchMessage()`を呼び出し
- プリセット適用時に`showPresetMessage()`を呼び出し
- リセット/刺激注入時にメッセージを表示

#### `src/ui/updateMetricsUI.js`
- システム状態バッジ変更時に`showStateChange()`を呼び出し
- 統合指標(Φ)の変化を監視
- モードとアクション状態の変化を監視（フレーム間隔を設けてスパム防止）

#### `src/ui/GuidePanel.js`
- 重要なネットワークイベント発生時に`showNetworkEvent()`を呼び出し
  - White Engine（統合傾向上昇）
  - Heartbeat（同期パルス）
  - Tension Release（緊張解放）
  - Eye Active（自己観測活性化）

## 📱 モバイル対応

### 位置調整の理由

モバイルでは画面下部がコントロールパネルとヘルプボタンで占有されているため、観測表示パネルは画面中央に配置しました：

```css
@media (max-width: 768px) {
    #observation-display {
        bottom: auto;
        top: 50%;
        transform: translate(-50%, -50%);
    }
}
```

これにより：
- コントロールパネルと重ならない
- ヘルプボタン（?）と重ならない
- タッチ操作の邪魔にならない（pointer-events: none）
- 視認性が高い

## 🎨 デザイン原則

1. **非侵襲的**: `pointer-events: none`でタッチ操作を妨げない
2. **一時的**: メッセージは自動的に消える（2〜4秒）
3. **文脈依存**: イベントの種類に応じて色とメッセージを変更
4. **読みやすさ**: 日本語フォント、適切なコントラスト、十分なサイズ
5. **外部観測**: システムの内部状態を「観察者」として説明

## 🧪 テスト方法

### 手動テスト

1. **タッチテスト**:
   - 画面をタップ → 「刺激が伝わっています...」などのメッセージが表示されることを確認

2. **プリセットテスト**:
   - 「シュマン」ボタンをタップ → プリセットの説明が表示されることを確認
   - 「黄金比」、「ガンマ」も同様にテスト

3. **システム状態テスト**:
   - システムが活動状態になると → 状態変化のメッセージが表示されることを確認
   - 臨界状態に達すると → 臨界メッセージが表示されることを確認

4. **リセット/刺激注入テスト**:
   - 「リセット」ボタンをタップ → リセットメッセージが表示されることを確認
   - 「刺激注入」ボタンをタップ → 注入メッセージが表示されることを確認

### ビルドテスト

```bash
npm run build
```

✅ ビルド成功を確認済み

## 📊 影響範囲

### 変更あり（UI/UXのみ）
- `index.html`: CSS + HTML構造
- `src/main.ts`: 初期化コード
- `src/perception/pointerHandlers.js`: イベントハンドラー統合
- `src/ui/updateMetricsUI.js`: 状態監視統合
- `src/ui/GuidePanel.js`: イベント統合
- `src/ui/ObservationDisplay.js`: 新規ファイル

### 変更なし（計算ロジック）
- `src/core/`: すべてのコア計算ロジック
- `src/organism/`: 生物的メカニズム
- `src/perception/TouchMemory.js`: タッチメモリロジック
- `src/constants/`: 定数値
- テスト: すべてのテストファイル

## ✅ 完成チェックリスト

- [x] 日本語観測表示システムを実装
- [x] タッチ時の説明を追加
- [x] システム状態変化の説明を追加
- [x] プリセット適用時の説明を追加
- [x] ネットワークイベント時の説明を追加
- [x] モバイルでの位置調整（コントロールパネルと重ならない）
- [x] デスクトップでの位置調整
- [x] ビルドが成功することを確認
- [x] 数値を一切変更していないことを確認
- [x] ドキュメントを作成

## 🎯 今後の拡張可能性

現在の実装は最小限の外部観測機能ですが、以下の拡張が可能：

1. **ユーザー設定**: 観測表示のON/OFF切り替え
2. **詳細度調整**: 簡潔モード/詳細モードの選択
3. **履歴表示**: 過去のイベントログを表示
4. **多言語対応**: 英語などの他言語サポート
5. **カスタムメッセージ**: ユーザー定義のメッセージテンプレート

---

生成日時: 2026-04-23
変更タイプ: UI/UX改善（計算ロジックには影響なし）
